PipelineSecrets: {}
cleanup:
  Annotations:
    helm.sh/hook: pre-delete
    helm.sh/hook-delete-policy: hook-succeeded
  Args:
  - --cleanup
expose:
  Annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: hook-succeeded
  Args:
  - --v
  - 4
  config:
    domain: halogo.hakt.com.au
    exposer: Ingress
    http: "true"
admin:
  authUrl: halogo.jx-uat.halogo.hakt.com.au/auth
jenkins:
  Servers:
    Global: {}
prow: {}
